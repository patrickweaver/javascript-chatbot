// To Do:
// Reset Button
// Reads name and sends it back to you in another question
// tone.js sounds
// Responsive:
// -- Remove phone style borders on small screens
// -- Fix avatars on small screens
// -- Shrink text on small screens
// Gradient at top when scrolling either chat or text areas.

/*

  // Look at the specialChats array in script.js to see if the uesr's input matches any of the special chats.
  // Convert the user's input to upper case only using .toUpperCase() to make the search more flexible.
  // If a match is found tell the robot to go off script by changing sendSpeicalChat[0] to true and sendSpecialChat[1] to the matched SpecialChats message.
  for (i = 0; i < specialChats.length; i+=1) {
    prompt = specialChats[i][0];
    response = specialChats[i][1];
    if (nextMessage.message.toUpperCase() == prompt) {
      console.log("🔎 Special Chat match found, going off script!");
      sendSpecialChat = [true, response];
      break
    }
  }
  
  
*/